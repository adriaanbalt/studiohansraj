<div id='details'>

	<ul class='clearfix hide'>
	{{#each gallery }}
	<li>
		<img src="{{ this }}"/>
	</li>
	{{/each}}
	</ul>

	<header>
		<h2>{{ name }}</h2>
	</header>
	
	<div class='information'>

		{{#if client }}
		{{#if client_url }}
		<p>Client: <a href="{{ client_url }}" target="_blank">{{ client }}</a></p>
		{{else}}
		<p>Client: <span>{{ client }}</span></p>
		{{/if}}
		{{/if}}
		
		{{#if director }}
		{{#if director_url }}
		<p>Director: <a href="{{ director_url }}" target="_blank">{{ director }}</a></p>
		{{else}}
		<p>Director: <span>{{ director }}</span></p>
		{{/if}}
		{{/if}}

		{{#if photographer }}
		{{#if photographer_url }}
		<p>Photographer: <a href="{{ photographer_url }}" target="_blank">{{ photographer }}</a></p>
		{{else}}
		<p>Photographer: <span>{{ photographer }}</span></p>
		{{/if}}
		{{/if}}
		
		{{#if stylist }}
		{{#if stylist_url }}
		<p>Stylist: <a href="{{ stylist_url }}" target="_blank">{{ stylist }}</a></p>
		{{else}}
		<p>Stylist: <span>{{ stylist }}</span></p>
		{{/if}}
		{{/if}}
		
		{{#if makeup }}
		{{#if makeup_url }}
		<p>Makeup: <a href="{{ makeup_url }}" target="_blank">{{ makeup }}</a></p>
		{{else}}
		<p>Makeup: <span>{{ makeup }}</span></p>
		{{/if}}
		{{/if}}
		
		{{#if hair }}
		{{#if hair_url }}
		<p>Hair: <a href="{{ hair_url }}" target="_blank">{{ hair }}</a></p>
		{{else}}
		<p>Hair: <span>{{ hair }}</span></p>
		{{/if}}
		{{/if}}
		
		{{#if talent }}
		{{#if talent_url }}
		<p>Talent: <a href="{{ talent_url }}" target="_blank">{{ talent }}</a></p>
		{{else}}
		<p>Talent: <span>{{ talent }}</span></p>
		{{/if}}
		{{/if}}
	</div>
</div>
